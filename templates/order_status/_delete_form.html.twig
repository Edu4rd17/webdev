<form method="post" action="{{ path('app_order_status_delete', {'id': order_status.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order_status.id) }}">
    <button class="btn">Delete</button>
</form>
