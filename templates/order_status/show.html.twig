{% extends 'base.html.twig' %}

{% block title %}OrderStatus{% endblock %}

{% block body %}
    <h1>OrderStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_status.id }}</td>
            </tr>
            <tr>
                <th>StatusName</th>
                <td>{{ order_status.statusName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_order_status_index') }}">back to list</a>

    <a href="{{ path('app_order_status_edit', {'id': order_status.id}) }}">edit</a>

    {{ include('order_status/_delete_form.html.twig') }}
{% endblock %}
