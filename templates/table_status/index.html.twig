{% extends 'base.html.twig' %}

{% block title %}TableStatus index{% endblock %}

{% block body %}
    <h1>TableStatus index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for table_status in table_statuses %}
            <tr>
                <td>{{ table_status.id }}</td>
                <td>{{ table_status.name }}</td>
                <td>
                    <a href="{{ path('app_table_status_show', {'id': table_status.id}) }}">show</a>
                    <a href="{{ path('app_table_status_edit', {'id': table_status.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_table_status_new') }}">Create new</a>
{% endblock %}
