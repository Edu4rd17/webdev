{% extends 'base.html.twig' %}

{% block title %}TableStatus{% endblock %}

{% block body %}
    <h1>TableStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ table_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ table_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_table_status_index') }}">back to list</a>

    <a href="{{ path('app_table_status_edit', {'id': table_status.id}) }}">edit</a>

    {{ include('table_status/_delete_form.html.twig') }}
{% endblock %}
